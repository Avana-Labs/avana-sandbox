# Wallet pill truncates to "TEST WAL…"

**Priority:** LOW · **Area:** ui

The header wallet pill truncates the label awkwardly ("TEST WAL…"). Widen the pill or shorten/format the label (e.g. show the address chip).

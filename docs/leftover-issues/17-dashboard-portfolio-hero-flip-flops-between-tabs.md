# Dashboard portfolio hero flip-flops between tabs

**Priority:** MEDIUM · **Area:** data

**Problem:** The dashboard hero total reads `$11,196` on the Lend & Multiply tabs but `$10,181` on the Borrow tab — the headline number changes when you switch tabs.

**Fix:** Show one stable, clearly-labeled portfolio total (or label the borrow-scope value distinctly so it doesn't read as "your net worth").

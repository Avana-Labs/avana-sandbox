export const DASHBOARD_SNAPSHOT_SURFACE_CLASS = "rounded-radius-md border-0 bg-card shadow-none"

/**
 * The single scrim behind every popup and full-screen overlay (dialogs, the desktop mega-menu,
 * mobile sheets). A light tint and a slight blur keep the page readable behind the popup.
 */
export const OVERLAY_SCRIM_CLASS = "bg-black/15 backdrop-blur-[2px]"
